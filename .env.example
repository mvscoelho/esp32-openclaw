# esp32-openclaw Plugin – Environment Variables

# WebSocket URL of esp32-openclaw-server /plugin endpoint
ESP32_SERVER_URL=ws://localhost:8989/plugin

# Bearer token for server authentication (leave empty for dev/no-auth)
ESP32_APP_TOKEN=

# Default OpenClaw Agent ID
ESP32_DEFAULT_AGENT=main
